import { ClipboardList } from 'lucide-react'
import { PlaceholderPage } from './PlaceholderPage'

export default function OrdersPage() {
  return (
    <PlaceholderPage
      icon={ClipboardList}
      title="Buyurtmalar"
      description="Sahifalangan, filtrlangan buyurtmalar jadvali (status, tarif, xizmat turi, kechikkan buyurtmalar) shu yerda bo'ladi."
      phase="Faza 5 — Web admin: CRM yadrosi"
    />
  )
}
